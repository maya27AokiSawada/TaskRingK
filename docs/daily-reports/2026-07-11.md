# 日報 2026-07-11

## 作業概要

Phase 6（QR招待）・Phase 7（通知）・Phase 8（ホワイトボード）を連続で実装し、
本日は Phase 8 の同期品質と描画ツール強化（色/線幅/消しゴム/線端）まで完了。

---

## 実施内容

### 1. Phase 6: QR招待

- 招待コード生成/解析ユーティリティを追加
- Firestore 招待 DataSource / Repository / UseCase を追加
- グループ画面に招待共有・カメラスキャンUIを接続

### 2. Phase 7: 通知

- 通知 DTO / Mapper / Firestore DataSource を追加
- 通知 Repository / UseCase / ViewModel / 画面を追加
- グループ削除/離脱、リスト作成/削除に通知発火を追加

### 3. Phase 8: ホワイトボード

- Whiteboard Repository（Hybrid）を追加し Firestore 同期を接続
- `SharedGroups/{groupId}/whiteboards/{whiteboardId}/strokes` を採用
- ストローク監視・保存・削除・全削除（batch delete）を実装
- WhiteboardViewModel をローカル保持からリモート購読ベースに移行
- Whiteboard UI を追加し Main タブに統合

### 4. Phase 8 追加改善（本日）

- Repository テストを追加
  - online/offline 切替
  - Firestore エラー時 Room フォールバック
  - stroke mutation 委譲
- 描画ツールを強化
  - 色選択
  - 線幅スライダー
  - 消しゴム ON/OFF
  - 線端スタイル（ROUND/BUTT）
- `flatMapLatest` 利用箇所の opt-in 警告を解消

---

## 検証ログ

実行コマンド:

```text
./gradlew :app:compileDevDebugKotlin
./gradlew :app:assembleDevDebug
./gradlew :app:testDevDebugUnitTest --tests "*WhiteboardViewModelTest"
./gradlew :app:testDevDebugUnitTest --tests "*HybridWhiteboardRepositoryImplTest"
```

結果:

- すべて BUILD SUCCESSFUL
- WhiteboardViewModel / HybridWhiteboardRepository の追加テストは成功

---

## 課題・メモ

- 開発環境では JDK 21 を固定すること（JDK 25 は Kotlin DSL で不整合発生）
- dev フレーバーは `app/src/dev/google-services.json` が必須

---

## 次の作業候補

- Firestore ルールとの整合確認（whiteboards / strokes のアクセス制御）
- Whiteboard 同期の実機2端末検証（オフライン復帰含む）
- 設定画面（Phase 9）の本実装
