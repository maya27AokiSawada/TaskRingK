# 日報 2026-07-14

## 作業概要

公開準備と Phase 8 の残件対応を実施。

- セキュリティチェックの自動化（ローカル + CI）
- Branch protection の自動適用スクリプト追加と適用
- Firestore ルールの整備と反映
- Phase 8 2端末検証基盤の整備
- 招待受諾時の権限エラー修正

---

## 1. セキュリティ / CI

- `scripts/security-preflight.ps1` を追加
  - 追跡禁止ファイル検出（`.env`, `google-services.json`, keystore など）
  - 追跡ファイル内の高リスクパターンスキャン
  - Git 履歴のキー痕跡チェック
- `.github/workflows/security-checks.yml` を追加
  - PR / main push で preflight と gitleaks を実行
- `scripts/apply-branch-protection.ps1` を追加
  - main の必須チェック・レビュー要件を API で適用
  - 403 時の案内を改善

結果:

- Branch protection 適用完了（公開化後に成功）
- required status checks が有効化済み

---

## 2. Firestore ルール整備

- `firebase/firestore.rules` を追加し、`SharedGroups` / `whiteboards` / `strokes` / `Notifications` のアクセス制御を定義
- `firebase.json` と `firebase/firestore.indexes.json` を追加
- `scripts/deploy-firestore-rules.ps1` を追加し、プロジェクト指定で反映可能化

結果:

- `goshopping-48db9` へ rules のデプロイ成功

---

## 3. Phase 8 検証基盤

- `scripts/phase8-validation.ps1` を追加
  - Whiteboard 主要テスト実行
  - 2端末接続チェック
  - Gradle 失敗時に終了コードで失敗化
- `docs/testing/phase8_validation.md` を追加
  - 2端末同期・オフライン復帰・ルール整合の手動チェック項目を明文化

検証ログ:

- JDK 21 でテスト成功
- Pixel 9 + Small_Phone 接続で 2端末条件を満たすことを確認

---

## 4. 招待受諾エラー対応

現象:

- 受諾時に `PERMISSION_DENIED` が発生

対応:

- `FirestoreInvitationDataSource.acceptInvitation` を修正
  - 受諾前の `SharedGroups` 読み取りを廃止
  - `arrayUnion` ベースの更新に変更して read 権限依存を排除
- `firebase/firestore.rules` の受諾更新条件を調整
  - 受諾に必要な変更キーのみ許可

追加調査:

- Pixel 側で `Unable to resolve host firestore.googleapis.com` を確認
- 受諾成功表示でもネットワーク不通時はクラウド同期が遅延/不達になることを確認

---

## 5. ドキュメント更新

- `README.md` に以下を追記
  - 公開前機密チェック手順
  - Branch protection 適用手順
  - Firestore ルール反映手順
  - Phase 8 検証手順への導線
- `docs/development_plan/kotlin_migration_master_plan.md` に 2026-07-14 進捗メモを追加

---

## 現在の状態

- セキュリティ対策: 実装済み
- ルール反映: 実施済み
- Phase 8 自動検証: 実施済み
- 2端末手動検証: 実施中（通信状態の影響あり）
